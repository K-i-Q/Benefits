@model IEnumerable<Domain.Empresa>

@{
    ViewData["Title"] = "Empresas";
}

<h2>Empresas</h2>

    <a class="nav-link" asp-controller="Cliente" asp-action="Index">Index</a>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cnpj)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Razao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cnpj)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Razao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone)
            </td>
            <td>
                <a asp-action="EmpresaParceria" asp-route-id="@item.EmpresaId">Adicionar</a> |
                <a asp-action="EmpresaEdit" asp-route-id="@item.EmpresaId">Edit</a> |
                <a asp-action="EmpresaDetails" asp-route-id="@item.EmpresaId">Details</a> |
                <a asp-action="EmpresaDelete" asp-route-id="@item.EmpresaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
